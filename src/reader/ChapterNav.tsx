import type { Chapter } from '../types';
interface Props { chapters: Chapter[]; active?: Chapter; disabled: boolean; onSelect: (chapter: Chapter) => void }
export function ChapterNav({chapters,active,disabled,onSelect}: Props) {
  return <details className="chapter-nav" open><summary>Capítulos y secciones <small>{chapters.length}</small></summary><nav aria-label="Capítulos">{chapters.map(chapter => <button key={chapter.id} disabled={disabled} onClick={() => onSelect(chapter)} aria-current={active?.id === chapter.id ? 'location' : undefined} style={{paddingLeft:12+Math.min(chapter.level,3)*12}}><span>{chapter.title}</span><small>P. {chapter.pageLabel}{chapter.kind !== 'content' ? ' · opcional' : ''}</small></button>)}</nav><p>{chapters.some(c => c.source === 'outline') ? 'Estructura tomada de los marcadores del PDF.' : chapters.some(c => c.source === 'detected') ? 'Encabezados detectados automáticamente. Pueden necesitar revisión.' : 'Este PDF no tiene capítulos identificables.'}</p></details>;
}
